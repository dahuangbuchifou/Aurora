# M2-003B Gate 2 实施启动指令 V1.0

> 建议临时路径：`临时文件/20260714_M2-003B_Gate2_实施启动指令_V1.0.md`  
> 状态：TASK_SPEC_APPROVED / IMPLEMENTATION_START  
> 技术负责人：大G  
> 方向监控与独立检查：婉儿  
> 项目负责人：大黄

## 1. 审阅结论

婉儿审阅结果：

```text
TASK_SPEC_APPROVED
BLOCKER：0
MAJOR：0
MINOR：0
Checklist修改：0
```

M2-003B Gate 2 可以正式进入实施。

## 2. 执行环境分工

服务器作为主要执行环境和交付事实来源。

婉儿负责：

- 分支操作；
- 依赖与测试执行；
- Git Commit 和 PR；
- 服务器资源观察；
- 交付证据收集；
- 独立 Review。

大黄本地 `E:\Aurora` 作为：

- 偶发复验环境；
- 服务器资源不足时的备用执行环境；
- 关键结果交叉验证环境；
- 紧急恢复与排查环境。

不得在服务器和本地同时修改同一任务分支。

切换到本地执行前，婉儿必须提供：

```text
当前Branch
当前HEAD Commit
未提交文件
失败命令
资源问题
建议由本地执行的具体步骤
```

## 3. GitHub 临时文件目录约定

`临时文件/` 是大G与婉儿之间的临时交接区，不是正式文档目录。

允许放置：

```text
实施启动指令
Review请求
补验说明
裁决请求
临时报告
待归档Markdown
小型文本清单
```

禁止放置：

```text
PAT、API Key、Cookie
.env
真实敏感材料
数据库
完整虚拟环境
大体积源码ZIP
生产代码的唯一副本
```

文件命名：

```text
YYYYMMDD_<TaskID>_<DocumentType>_Vx.y.md
```

生命周期：

```text
大G提供临时文件
→ 婉儿读取并确认
→ 归档到正式docs路径
→ Commit进入任务分支
→ 临时文件删除
```

## 4. 实施前操作

```bash
git switch main
git pull --ff-only origin main
git status --short
git rev-parse HEAD
```

工作区必须为空。

创建或切换任务分支：

```bash
git switch -c feature/m2-003b-gate2-cognitive-safety
```

若分支已存在：

```bash
git switch feature/m2-003b-gate2-cognitive-safety
git rebase main
```

记录：

```text
base_commit
branch_name
head_commit
merge_base
```

## 5. 冻结资产

实施前生成：

```text
docs/qa/M2-003B_Gate2_frozen_assets.sha256
```

至少包含：

```text
schemas/v1/
schemas/v1_1/
schemas/extraction/v1/
scripts/gate0_check.py
Gate 0最终报告
Gate 1报告
Gate 1 ContextWindow Hash契约
Gate 1 QuoteGate契约
Gate 1真实垂直切片Fixture
```

未授权修改冻结资产视为 BLOCKER。

## 6. Gate 2 实施范围

建立七类独立对抗 Fixture：

```text
prediction_pollution
valuation_recommendation
prompt_injection
fake_quote
forged_or_outside_unit
high_confidence_pollution
provider_independence_override
```

实现或扩展：

```text
SafetyGate
CandidatePolicyValidator
ValidationFinding
ExtractionEnvelope验证
ReviewBundle拒绝记录
```

具体内部命名由大G决定。

## 7. Provider 越权字段策略

冻结方案：

```text
严格拒绝
+
明确 ERROR 级 ValidationFinding
```

越权字段至少包括：

```text
independence_group
source_quality_tier
evidence_strength
verification_status
epistemic_status
```

不得静默接受、静默删除或覆盖 Aurora 规则。

## 8. 六项硬门禁

```text
G2-1 Fact污染率 = 0
G2-2 虚假Quote接受率 = 0
G2-3 非法Unit接受率 = 0
G2-4 Prompt Injection越权率 = 0
G2-5 Provider越权字段接受率 = 0
G2-6 高confidence改变知识状态次数 = 0
```

任一失败：

```text
Gate 2 = FAIL
不得进入 M2-003C
```

## 9. 实施红线

本任务不得加入：

```text
远程Provider
真实LLM
persist_drafts
apply_review
Fact持久化
认知对象数据库写入
CLI交互式Review
Web UI
跨Document知识合并
```

不得修改核心 V1.1 Schema，不得新增 Alembic Revision。

## 10. 测试与确定性

至少新增：

```text
tests/unit/test_safety_gate.py
tests/integration/test_m2_003b_gate2_cognitive_safety.py
```

每个对抗 Fixture 重复10次，必须保持：

```text
Candidate排序一致
Finding排序一致
accepted/rejected集合一致
ReviewBundle Hash一致
```

全量门禁：

```text
0 failed
0 warnings
总Coverage >= 90%
Gate 2新增模块Coverage >= 90%
```

## 11. 项目文档更新

任务正式启动后更新项目看板：

```text
M2-003B / Gate 2：
READY_FOR_TASK_SPEC_CONFIRMATION
→ IN_PROGRESS
```

总体治理进度暂保持：

```text
42%
```

待优化事项：

```text
OPT-064 冻结资产保护
→ IN_PROGRESS

OPT-066 Provider越权字段处理策略
→ APPROVED / IN_PROGRESS

OPT-065 Extraction CLI零覆盖率
→ DEFERRED_BY_SCOPE

OPT-067 真实模型Prompt Injection验证
→ DEFERRED / M2-003D
```

## 12. 大G交付前自检

只有以下条件全部满足，才能标记：

```text
READY_FOR_REVIEW
```

- Gate 2 Checklist完成；
- 六项硬门禁全 PASS；
- 原任务缺陷已修复；
- 全量测试通过；
- Coverage通过；
- 冻结资产Hash一致；
- 核心Schema Diff为空；
- Alembic无变化；
- BLOCKER为0；
- Changed Files、Commit和证据齐全。

## 13. Round 1

```text
大G自检
→ 婉儿独立检查
→ 双方联合收敛
→ 形成 Round 1 共同记录
```

Round 1 直接 PASS 时，不执行 Round 2。

## 14. 下一步

婉儿收到本指令后：

1. 更新项目看板和待优化事项；
2. 生成冻结资产Hash；
3. 确认任务分支；
4. 启动 Gate 2 实施；
5. 实施完成后按协作规范 V2.1 提交完整交付；
6. 不在实施中扩大任务范围。
