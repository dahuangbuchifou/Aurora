# M2-001 QA 验收清单 V1.0

## 1. 环境

记录：

```text
OS
Python
SQLite
Pydantic
SQLAlchemy
Alembic
pytest
```

## 2. 完整性

```bash
sha256sum -c SHA256SUMS.txt
pytest --collect-only -q
```

## 3. 全量测试

```bash
pytest -q
pytest \
  --cov=aurora \
  --cov-branch \
  --cov-report=term-missing \
  --cov-fail-under=90
```

## 4. CLI验收

分别执行：

- Markdown 新导入；
- Markdown 重复导入；
- 纯文本；
- Structured Segments；
- dry-run；
- reject；
- new_version；
- 超限文件；
- 非UTF-8文件；
- `--output json`。

## 5. 数据库核查

确认：

- 同一 Source 不因不同 Document 内容重复创建；
- 相同内容同 Parser 复用 Document；
- Parser 版本变化创建新 Document；
- Document + ContentUnits 原子提交；
- 失败 ProcessingRun 保留；
- dry-run 数据库无变化；
- ContentUnit ID 与顺序稳定；
- Provenance 可回到 Document 和 ProcessingRun。

## 6. 兼容核查

- M1 全部测试继续通过；
- 17类核心对象数量不变；
- `schemas/v1/` 与 `schemas/v1_1/` 无变更；
- V1.0/V1.1混合数据库可读取；
- 无新 Alembic Revision。

## 7. QA输出

必须返回：

```text
上一轮工作评估
完整测试数
Coverage
三类输入结果
幂等结果
事务回滚结果
ProcessingRun状态
CLI JSON结果
Bug与修复
OPT更新
PASS / FAIL
```
