# M2-003C Gate 3：草案持久化验证任务卡 V0.1

> 状态：TASK_SPEC_APPROVED · IMPLEMENTATION_READY  
> 前置节点：Gate 2 OWNER_DIRECTED_CONDITIONAL_CLOSURE  
> 目标：验证Entity、DataPoint、Claim、Evidence草案的安全、原子、幂等持久化。
> 审阅：婉儿 CONDITIONAL_PASS → 大G 冻结 M-C3-01/MIN-C3-01 → TASK_SPEC_APPROVED

## 1. 目标链

```text
已验证ReviewBundle
→ Persistence Mapper
→ Draft Transaction
→ Entity / DataPoint / Claim / Evidence
→ Repository / SQLite
→ ProcessingRun审计
```

FactCandidate只保留在ReviewBundle，不写入核心Fact。

## 2. 强制前置修复

### 阶段一：OPT-069（编码前·首个Commit）

```text
FixtureProvider._build_candidate 不再读取：
- independence_group
- promotable
- promotable_to_fact

Raw payload 越权字段 → ERROR finding
```

Commit示例：`fix: remove provider-owned epistemic fields before Gate 3 persistence`

### 阶段二：OPT-068/070/071（Gate 3 Review Round 1前）

正式Provider编排链：
```text
FixtureProvider → ProviderResponse → Provider Decoder
→ ExtractionEnvelope → QuoteGate → SafetyGate → ReviewBundle
```

统：统一Provider Decoder入口
OPT-071：promotable_to_fact所有权（Provider设置→ERROR，Aurora计算eligible_for_review，ReviewDecision批准）

## 3. 范围内

- ReviewBundle输入校验；
- Bundle SHA-256校验；
- accepted candidates映射；
- Entity/DataPoint/Claim/Evidence草案写入；
- `HumanReview=NOT_REVIEWED`；
- Claim默认`UNDER_REVIEW`；
- Engine-side independence group；
- 稳定对象ID；
- 幂等重跑；
- 单事务原子提交；
- ProcessingRun独立记录；
- dry-run；
- 失败回滚；
- 不写Fact。

## 4. 范围外

```text
Fact创建
apply_review
远程Provider
真实LLM
知识图谱
Insight
PersonalOpinion
Web UI
```

## 5. Gate 3硬门禁

```text
G3-1 事务残留对象 = 0
G3-2 自动Fact = 0
G3-3 未复核Claim被标为confirmed = 0
G3-4 悬空Candidate引用 = 0
G3-5 悬空核心对象引用 = 0
G3-6 重复运行产生严格重复对象 = 0
G3-7 Provider越权字段进入持久化对象 = 0
```

## 6. 持久化状态

```text
Entity/DataPoint/Evidence：
  provenance.human_review = NOT_REVIEWED

Claim：
  epistemic_status = UNDER_REVIEW
  provenance.human_review = NOT_REVIEWED

Fact：
  禁止创建
```

## 7. 事务要求

```text
Entity + DataPoint + Claim + Evidence
→ 一个业务事务
```

任意映射或写入失败：

```text
全部回滚
ProcessingRun独立保留FAILED
```

## 8. 幂等要求

稳定身份不得使用随机UUID作为业务幂等键。

建议：

```text
candidate stable identity
+ review_bundle_sha256
+ workspace_id
+ object_type
```

重复执行同一Bundle：

```text
reuse
不制造重复对象
```

## 9. ReviewBundle门禁

持久化前必须验证：

- bundle_sha256；
- ReviewBundle不可变；
- Candidate在accepted列表；
- Candidate不在rejected列表；
- Finding中无目标Candidate ERROR；
- source_unit_id存在；
- workspace和document一致；
- Bundle来自允许的Provider和Profile。

## 10. 冻结资产

冻结：

```text
Gate 0资产
Gate 1契约
Gate 2七类Fixture和报告
核心V1.1 Schema
现有Alembic
```

## 11. 测试要求

至少包含：

- 正常Bundle草案写入；
- 同一Bundle重复运行；
- 中途失败回滚；
- rejected candidate不写入；
- FactCandidate不写入；
- Provider越权字段不写入；
- Claim状态保守；
- independence group由引擎生成；
- dry-run；
- ProcessingRun失败审计；
- 全量回归；
- Coverage≥90%。

## 12. 联合确认结果

婉儿 CONDITIONAL_PASS → 大G修复 → TASK_SPEC_APPROVED

**M-C3-01 幂等键已冻结：**

Bundle操作键：`SHA256(namespace + workspace_id + review_bundle_sha256)`

草案对象自然键：`SHA256(namespace + workspace_id + object_type + candidate_stable_identity)`

candidate_stable_identity由以下生成（排除Provider越权字段）：
```text
candidate_type + candidate_id + document_id + source_unit_ids + 规范化语义字段
```

**MIN-C3-01 模块目录已冻结：**
```text
src/aurora/persistence/
├── contracts.py
├── identity.py
├── validation.py
├── mapper.py
└── draft_service.py

src/aurora/workflow/
└── draft_persistence.py
```

**实施分三阶段：**
1. OPT-069 前置修复（首个Commit）
2. 正式Provider编排链（关闭OPT-068/070/071）
3. 草案持久化主实现
